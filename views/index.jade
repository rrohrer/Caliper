extends layout

block content
  h1= title
  ol
    each record in records
      li
        a(href='view/' + record.id)
          = 'v' + record.version + ' ' + record.time.toLocaleString()
  ul
    li Menu
    li
      a(href='symbol')
        = 'symbols'
